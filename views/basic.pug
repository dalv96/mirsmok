doctype
html
    head
        block title
            title Система мониторинга отзывов клиентов
        block head
            meta(name='viewport' content='width=device-width, initial-scale=1')
            link(rel='stylesheet' href='/public/css/bootstrap.min.css')
            link(rel='stylesheet' href='/public/css/basic.css')
            script(src='/public/js/jquery-3.2.0.min.js')
            
    body
        header
            block header
                if(__user)
                    nav
                        ul.menu
                            li: a(href='/' tabindex='990') 
                                span Главная страница
                            if(__user.role == 0)
                                li: a(href='/admin/users' tabindex='991') Пользователи
                            li: a(href='/info' tabindex='992') Статистика
                            li.profile: a(href='/profile' tabindex='993') Мой профиль
                            li.exit: a(href='/logout' tabindex='994') Выйти
        div
            block content
        footer
            .developBy
                p Design by 
                    a(href='http://www.miranda-media.ru/') Miranda-Media 
                    | in 2017
